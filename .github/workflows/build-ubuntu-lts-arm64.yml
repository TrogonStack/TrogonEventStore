name: Ubuntu LTS ARM64

on:
  pull_request:
  merge_group:
    types:
      - checks_requested
  push:
    branches:
      - master
      - release/v*
    tags:
      - v*

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: Changes
    permissions:
      contents: read
      pull-requests: read
    uses: ./.github/workflows/changes-reusable.yml

  build:
    needs: changes
    if: ${{ needs.changes.outputs.code == 'true' }}
    name: Build
    uses: ./.github/workflows/build-reusable.yml
    with:
      os: ubuntu-24.04-arm
      arch: arm64
    secrets: inherit

  required:
    name: ARM64 CI
    if: ${{ always() }}
    needs:
      - changes
      - build
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - name: Require successful jobs
        env:
          REQUIRED_JOBS: ${{ toJSON(needs) }}
          EXPECTED_RESULT: ${{ needs.changes.outputs.code == 'true' && 'success' || 'skipped' }}
        run: |
          jq -e --arg expected "$EXPECTED_RESULT" '
            .changes.result == "success" and
            (del(.changes) | all(.[]; .result == $expected))
          ' <<< "$REQUIRED_JOBS"
