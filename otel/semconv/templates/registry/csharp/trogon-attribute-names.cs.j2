// <auto-generated />

using System;
using System.Collections.Generic;

namespace TrogonEventStore.SemanticConventions
{
	public static class TrogonAttributeNames
	{
{% for group in ctx %}
{% for attribute in group.attributes | sort(attribute="name") %}
		public const string {{ attribute.name | pascal_case | regex_replace("^TrogonEventstore", "") }} = "{{ attribute.name }}";
{% endfor %}
{% endfor %}

		public static IReadOnlyList<string> All { get; } = Array.AsReadOnly(new[]
		{
{% for group in ctx %}
{% for attribute in group.attributes | sort(attribute="name") %}
			{{ attribute.name | pascal_case | regex_replace("^TrogonEventstore", "") }},
{% endfor %}
{% endfor %}
		});
	}
}{{ "\n" }}
