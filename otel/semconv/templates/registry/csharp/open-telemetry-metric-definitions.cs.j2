// <auto-generated />

using System;
using System.Collections.Generic;

namespace TrogonEventStore.SemanticConventions
{
	public static class OpenTelemetryMetricDefinitions
	{
{% for group in ctx %}
{% for metric in group.metrics | sort(attribute="metric_name") %}
		public static MetricDefinition {{ metric.metric_name | pascal_case }} { get; } = new MetricDefinition(
			"{{ metric.metric_name }}",
			"{{ metric.unit }}",
			"{{ metric.brief }}",
{% if metric.instrument == "updowncounter" %}
			MetricInstrumentKind.UpDownCounter);
{% else %}
			MetricInstrumentKind.{{ metric.instrument | pascal_case }});
{% endif %}
{% endfor %}
{% endfor %}

		public static IReadOnlyList<MetricDefinition> All { get; } = Array.AsReadOnly(new[]
		{
{% for group in ctx %}
{% for metric in group.metrics | sort(attribute="metric_name") %}
			{{ metric.metric_name | pascal_case }},
{% endfor %}
{% endfor %}
		});
	}
}{{ "\n" }}
